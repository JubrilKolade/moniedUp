# Example environment variables for MoniedUp backend
# Copy this file to `.env` and replace values before running the server:
# cp .env.example .env

# Database (PostgreSQL)
# Example (local):
DATABASE_URL="postgresql://username:password@localhost:5432/database_name?schema=public"

# If using Docker Compose and your postgres service is named `postgres`, use:
# DATABASE_URL="postgresql://username:password@postgres:5432/database_name?schema=public"

# JWT secret (required)
JWT_SECRET=your-secret-key-here-change-this-in-production

# Server options
PORT=3000
CORS_ORIGIN=http://localhost:3000
NODE_ENV=development

# Notes:
# - Do NOT add spaces around the `=` sign.
# - Quotes around DATABASE_URL are recommended to avoid parsing issues.
# - After editing, verify values with:
#   cd backend && node -e "require('dotenv').config(); console.log(process.env.DATABASE_URL, process.env.JWT_SECRET)"
